// Runtime configuration placeholder.
// In dev this stays empty (the app uses build-time VITE_* vars).
// In the Docker image, docker-entrypoint.sh overwrites this file from env vars.
window.__ENV__ = window.__ENV__ || {};
